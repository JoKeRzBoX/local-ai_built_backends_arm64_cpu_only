# local-ai_built_backends_arm64_cpu_only
Locally build backends for local-ai. Needed because no packages for arm64 CPU only were available.


Just clone this repo and than copy the folders from inside "backends" into your actul local-ai working folder backend's subdirectoy.

